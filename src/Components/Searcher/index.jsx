import React from "react";
import { Stack, TextField, IconButton } from "@mui/system";

const Searcher = () => {

    <Stack>
        <TextField></TextField>
        <IconButton></IconButton>
    </Stack>
};

export { Searcher };
